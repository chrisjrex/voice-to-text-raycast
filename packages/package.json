{
  "name": "voicekit-packages",
  "private": true,
  "workspaces": [
    "core",
    "cli",
    "cli-lite"
  ],
  "scripts": {
    "build": "npm run build --workspace=@voicekit/core && npm run build --workspace=@voicekit/cli",
    "build:core": "npm run build --workspace=@voicekit/core",
    "build:cli": "npm run build --workspace=@voicekit/cli",
    "build:cli-lite": "npm run build --workspace=@voicekit/cli-lite"
  }
}
